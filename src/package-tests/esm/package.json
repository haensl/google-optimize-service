{
  "name": "google-optimize-service-esm-module-test",
  "version": "1.1.8",
  "description": "",
  "scripts": {
    "test": "jest --runInBand --no-cache --ci --reporters='default' --reporters='jest-junit'"
  },
  "babel": {
    "presets": [
      [
        "@babel/preset-env"
      ]
    ]
  },
  "jest-junit": {
    "outputDirectory": "test-results/jest",
    "outputName": "results.xml",
    "suitName": "google-optimize-service esm module tests"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.8.4",
    "@babel/preset-env": "^7.8.4",
    "babel-jest": "^26.6.3",
    "jest": "^26.6.3",
    "jest-junit": "^12.0.0"
  }
}
